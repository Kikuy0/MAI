#include <iostream>
#include <string>

using namespace std;

int main() {
  int n, m;
  string s;
  cin >> n >> s >> m;
  for (int k = 0; k < m; ++k) {
    string a;
    int l;
    cin >> l >> a;
  }
}
